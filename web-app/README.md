# training_demos web-app

Minimal Node.js HTTP server for the training_demos workspace.

Run with the provided script:

```bash
cd web-app
./run.sh
```

Or with npm:

```bash
cd web-app
npm start
```

The server listens on `PORT` environment variable (default: `3000`).
